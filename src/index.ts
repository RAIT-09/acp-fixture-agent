/**
 * acp-fixture-agent — deterministic, scenario-driven ACP agent for testing
 * Agent Client Protocol clients.
 *
 * Library entry point. The in-process agent factory and the scenario
 * registry are exported from here.
 */

export {};
